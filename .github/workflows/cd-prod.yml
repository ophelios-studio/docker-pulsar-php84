name: CD - Prod 🚀

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/build-publish-docker.yml
    with:
      image_name: "docker-pulsar-php85"
      tag: "latest"
